Vamos começar instalando as seguintes bibliotecas:

```bash
pip install google-adk litellm fastapi uvicorn httpx pydantic openai streamlit requests
```

Configuração da chave de API:

``` bash
export OPENAI_API_KEY=""
```